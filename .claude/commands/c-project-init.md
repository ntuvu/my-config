---
description: 😏 Initialize CLAUDE.md and agent_docs/ for a project
argument-hint: [project-path]
---

Initialize Claude Code configuration for the current or specified project.

## Instructions

1. Set the target path: use `$ARGUMENTS` if provided, otherwise use the current working directory.
2. Invoke the `a-project-initializer` agent with the target path.
